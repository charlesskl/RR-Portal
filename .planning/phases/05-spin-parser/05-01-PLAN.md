---
phase: 05-spin-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/excel-parser.js
autonomous: true
requirements:
  - SPIN-01
  - SPIN-02
  - SPIN-03
  - SPIN-04
must_haves:
  truths:
    - "上传 SPIN Excel 后 detectFormat 返回 'spin' 而非 'plush'"
    - "导入后 MoldPart 表包含正确的料型、料重、机型、件数"
    - "导入后 SewingDetail 表包含正确的布料名称、用量、物料价"
    - "QuoteVersion.format_type 为 'spin'"
  artifacts:
    - path: "server/services/excel-parser.js"
      provides: "SPIN format detection, parsing, product_no extraction"
      contains: "return 'spin'"
  key_links:
    - from: "detectFormat"
      to: "parseWorkbook"
      via: "format variable controls moldStartRow and sewingDetails"
      pattern: "format === 'spin'"
    - from: "parseHeader"
      to: "import.js product_no"
      via: "header.product_no flows to Product.item_no"
      pattern: "总表"
---

<objective>
在 excel-parser.js 中添加 SPIN 格式支持：格式识别、产品编号提取、MoldPart 起始行、车缝明细解析。

Purpose: 使系统能正确导入 SPIN 内部报价明细 Excel
Output: 修改后的 excel-parser.js，支持 spin 格式的完整解析链路
</objective>

<execution_context>
@D:/Users/�/.claude/get-shit-done/workflows/execute-plan.md
@D:/Users//.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/services/excel-parser.js
@server/routes/import.js

<interfaces>
From server/services/excel-parser.js:
```javascript
// detectFormat (line 57) — returns 'plush' | 'injection', needs to add 'spin'
function detectFormat(workbook) {
  const sheetNames = workbook.worksheets.map(ws => ws.name);
  const hasPlushIndicator = sheetNames.some(n =>
    n.includes('车缝明细') || n.includes('搪胶')
  );
  return hasPlushIndicator ? 'plush' : 'injection';
}

// parseHeader (line 101) — extracts product_no from B1/C1
// For SPIN: product_no is in 总表 sheet, Row 8, Col 2

// parseMoldParts (line 168) — takes startRow param, dynamically finds header with 模号/名称
// SPIN header at Row 11, data at Row 12 → pass startRow=12

// parseSewingDetails (line 297) — starts scanning at row 4
// SPIN data starts at row 3 (header at row 2) → must adjust start row

// parseWorkbook (line 769) — orchestrator
// Line 800: moldStartRow = format === 'plush' ? 17 : 18
// Line 805: sewingDetails = format === 'plush' ? parseSewingDetails(workbook) : []
```

From server/routes/import.js:
```javascript
// Line 61: format_type is stored from data.format_type || 'injection'
// Line 66: same pattern for INSERT — already handles any string value
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 SPIN 格式识别和产品编号提取</name>
  <files>server/services/excel-parser.js</files>
  <read_first>server/services/excel-parser.js</read_first>
  <action>
修改 `detectFormat` 函数 (约 line 57)：在 hasPlushIndicator 判断之前，添加 SPIN 检测逻辑：

```javascript
function detectFormat(workbook) {
  const sheetNames = workbook.worksheets.map(ws => ws.name);

  // SPIN detection: has '装配' sheet and Row 2 of that sheet contains 'SPIN' (any column)
  const assemblySheet = workbook.worksheets.find(ws => ws.name === '装配');
  if (assemblySheet) {
    let hasSpinLabel = false;
    assemblySheet.getRow(2).eachCell(cell => {
      const v = strVal(cell) || '';
      if (v.includes('SPIN')) hasSpinLabel = true;
    });
    if (hasSpinLabel) return 'spin';
  }

  const hasPlushIndicator = sheetNames.some(n =>
    n.includes('车缝明细') || n.includes('搪胶')
  );
  return hasPlushIndicator ? 'plush' : 'injection';
}
```

修改 `parseHeader` 函数 (约 line 101)：在现有 product_no 提取逻辑之后，添加 SPIN 特殊处理。在函数签名中添加 workbook 参数：

将 `function parseHeader(ws, format)` 改为 `function parseHeader(ws, format, workbook)`

在 product_no 提取代码块之后（约 line 109 之后），添加：

```javascript
  // SPIN: product_no from 总表 sheet, Row 8, Col 2, format "货号：#29090"
  if (format === 'spin' && workbook) {
    const summarySheet = workbook.worksheets.find(ws => ws.name.includes('总表'));
    if (summarySheet) {
      const raw = strVal(summarySheet.getCell(8, 2)) || '';
      const m = raw.match(/\d+/);
      if (m) product_no = m[0];
    }
  }
```

更新 `parseWorkbook` 中 parseHeader 的调用 (约 line 781)：
将 `const header = parseHeader(ws, format);` 改为 `const header = parseHeader(ws, format, workbook);`
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('server/services/excel-parser.js','utf8'); console.log('SPIN detect:', s.includes(\"return 'spin'\")); console.log('总表 lookup:', s.includes('总表')); console.log('parseHeader workbook:', s.includes('parseHeader(ws, format, workbook)'));"</automated>
  </verify>
  <acceptance_criteria>
    - detectFormat 函数在 plush 判断之前扫描 '装配' sheet 的 Row 2 全行，检查是否含 'SPIN'
    - parseHeader 接受 workbook 参数，spin 格式时从 总表 sheet Row8 Col2 提取货号
    - parseWorkbook 调用 parseHeader 时传入 workbook
  </acceptance_criteria>
  <done>grep 'return .spin.' excel-parser.js 返回匹配行；grep '总表' excel-parser.js 返回匹配行</done>
</task>

<task type="auto">
  <name>Task 2: 启用 SPIN 的 MoldPart 和车缝明细解析</name>
  <files>server/services/excel-parser.js</files>
  <read_first>server/services/excel-parser.js</read_first>
  <action>
修改 `parseWorkbook` 函数中的 moldStartRow 逻辑 (line 800)：

将：
```javascript
const moldStartRow = format === 'plush' ? 17 : 18;
```
改为：
```javascript
const moldStartRow = format === 'spin' ? 12 : format === 'plush' ? 17 : 18;
```

注意：parseMoldParts 内部有动态 header 检测（搜索 "模号" + "名称"），所以即使 startRow 不精确也能自动修正。传 12 是给它一个合理的搜索范围。

修改 sewingDetails 行 (line 805)：

将：
```javascript
const sewingDetails = format === 'plush' ? parseSewingDetails(workbook) : [];
```
改为：
```javascript
const sewingDetails = (format === 'plush' || format === 'spin') ? parseSewingDetails(workbook) : [];
```

修改 `parseSewingDetails` 函数 (line 308)：

SPIN 的车缝明细 header 在 Row 2，data 从 Row 3 开始。当前代码 `for (let row = 4; ...)` 会跳过 Row 3。

将起始行改为动态检测：在 `let sortOrder = 0;` 之后（约 line 306），添加动态 header 检测并替换硬编码的 row=4：

```javascript
  // Dynamically find header row (contains 物料名称 or 裁片部位)
  let dataStartRow = 4; // default for plush
  for (let r = 1; r <= 5; r++) {
    const b = strVal(ws.getCell(r, 2));
    const c = strVal(ws.getCell(r, 3));
    if ((b && b.includes('物料名称')) || (c && c.includes('裁片部位'))) {
      dataStartRow = r + 1;
      break;
    }
  }

  for (let row = dataStartRow; row <= 100; row++) {
```

将原来的 `for (let row = 4; row <= 100; row++) {` 替换为上面的 `for (let row = dataStartRow; row <= 100; row++) {`

同时，为 SPIN 格式修改 laborItems 解析逻辑 (line 372)。当前只对 plush 格式做全表扫描人工行，spin 也需要：

将：
```javascript
  if (format === 'plush') {
```
改为：
```javascript
  if (format === 'plush' || format === 'spin') {
```
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('server/services/excel-parser.js','utf8'); console.log('moldStartRow spin:', s.includes(\"format === 'spin' ? 12\")); console.log('sewing spin:', s.includes(\"format === 'spin'\") && s.includes('parseSewingDetails')); console.log('dataStartRow:', s.includes('dataStartRow'));"</automated>
  </verify>
  <acceptance_criteria>
    - moldStartRow 对 spin 格式为 12
    - parseSewingDetails 对 spin 格式也会执行
    - parseSewingDetails 动态检测 header 行而非硬编码 row=4
    - laborItems 扫描对 spin 格式也启用
  </acceptance_criteria>
  <done>SPIN Excel 导入后 MoldPart 和 SewingDetail 表有正确数据</done>
</task>

</tasks>

<verification>
导入 SPIN 报价明细 Excel 文件后验证：

1. API 返回 success: true
2. 检查 QuoteVersion 表：format_type = 'spin'
3. 检查 Product 表：item_no 为从总表提取的货号数字（如 "29090"）
4. 检查 MoldPart 表：有数据行，料型/料重/机型/件数字段非空
5. 检查 SewingDetail 表：有数据行，fabric_name/usage_amount/material_price_rmb 字段非空

```bash
# 验证命令（需要先导入一个 SPIN 文件）
curl -s -F "file=@DATA/SPIN-sample.xlsx" http://localhost:3000/api/import | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const j=JSON.parse(d);console.log('success:',j.success,'versionId:',j.versionId)"
```
</verification>

<success_criteria>
- detectFormat 对 SPIN 文件返回 'spin'
- 产品编号从总表 sheet 正确提取
- MoldPart 数据从 Row 12 开始正确解析
- SewingDetail 数据从车缝明细 sheet 正确解析（包括 Row 3 的数据）
- format_type 字段存储为 'spin'
</success_criteria>

<output>
After completion, create `.planning/phases/05-spin-parser/05-01-SUMMARY.md`
</output>
