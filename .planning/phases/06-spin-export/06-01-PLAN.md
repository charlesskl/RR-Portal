---
phase: 06-spin-export
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/templates/VQ-template-spin.xlsx
  - server/services/spin-exporter.js
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-05]
must_haves:
  truths:
    - "SPIN 版本调用 exportSpinVersion 返回有效 Excel Buffer"
    - "款式 sheet 的 Fabric Cost 区域 (R23-R35) 填入了 SewingDetail 布料数据"
    - "款式 sheet 的 Packaging 区域 (R86-R97) 填入了 H-tag/CDU/Master carton 数据"
    - "款式 sheet 的 Labor 区域 (R126-R131) 填入了 Sewing/Cutting/Packing 工时工价"
    - "款式 sheet 的 Markup 区域 (R135-R139) 写入了 markup 百分比和小计引用"
    - "Summary sheet 基本信息已填写"
  artifacts:
    - path: "server/templates/VQ-template-spin.xlsx"
      provides: "SPIN Vendor Quote Form 模板文件"
    - path: "server/services/spin-exporter.js"
      provides: "SPIN 格式 Excel 导出服务"
      exports: ["exportSpinVersion"]
  key_links:
    - from: "server/services/spin-exporter.js"
      to: "server/services/db.js"
      via: "getDb() 查询版本数据"
      pattern: "getDb\\(\\)"
    - from: "server/services/spin-exporter.js"
      to: "server/templates/VQ-template-spin.xlsx"
      via: "ExcelJS readFile 加载模板"
      pattern: "VQ-template-spin"
---

<objective>
创建 SPIN Vendor Quote Form 导出服务：复制模板文件，实现 spin-exporter.js 填充款式 sheet 全部区域（Fabric、Packaging、Labor、Markup）和 Summary sheet。

Purpose: 实现 SPIN 版本的完整 Excel 导出能力
Output: VQ-template-spin.xlsx 模板文件 + spin-exporter.js 服务模块
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/services/excel-exporter.js
@server/routes/export.js

<interfaces>
From server/services/excel-exporter.js:
```javascript
// Pattern to follow — loadData, setVal, clearRows, r2 helpers
function loadData(versionId) { /* returns { version, product, params, moldParts, sewingItems, sewingLaborItems, packagingItems, ... } */ }
function setVal(ws, row, col, value) { /* skips formula cells, guards NaN */ }
function r2(v) { /* round to 2 decimals */ }
async function exportVersion(versionId) { /* loads template, fills sheets, returns buffer */ }
```

From server/services/db.js:
```javascript
function getDb() { /* returns better-sqlite3 instance */ }
```

QuoteParams schema fields: hkd_rmb_quote, hkd_rmb_check, rmb_hkd, hkd_usd, markup_body, markup_packaging, labor_hkd, box_price_hkd, markup_point
SewingDetail fields: fabric_name, position, cut_pieces, usage_amount, material_price_rmb, price_rmb, markup_point, total_price_rmb
PackagingItem fields: name, eng_name, pm_no, remark, moq, quantity, unit_price, new_price
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 复制 SPIN 模板文件并创建 spin-exporter.js</name>
  <files>server/templates/VQ-template-spin.xlsx, server/services/spin-exporter.js</files>
  <read_first>
    - server/services/excel-exporter.js
    - server/routes/export.js
  </read_first>
  <action>
**Step 1: 复制模板文件**

```bash
cp "c:/Users/dell/Desktop/Quoation for Projec#29090  T.94703-0001 PWP LPP - 6.88 Movie Plush Refresh-2026.3.15  (Royal Regent-ID).xlsx" server/templates/VQ-template-spin.xlsx
```

**Step 2: 创建 `server/services/spin-exporter.js`**

复用 excel-exporter.js 的模式（loadData, setVal, clearRows, r2, fixSharedFormulas），但针对 SPIN 模板布局。

```javascript
const ExcelJS = require('exceljs');
const path = require('path');
const { getDb } = require('./db');

const TEMPLATE_PATH = path.join(__dirname, '../templates/VQ-template-spin.xlsx');
```

**loadData 函数:** 与 excel-exporter.js 基本相同，查询:
- QuoteVersion, Product, QuoteParams
- `SewingDetail WHERE version_id = ? AND (position IS NULL OR position = '') ORDER BY sort_order` → fabricItems (布料)
- `SewingDetail WHERE version_id = ? AND position = '__labor__' ORDER BY sort_order` → laborItems (工时)
- `PackagingItem WHERE version_id = ? ORDER BY sort_order` → packagingItems

**setVal / r2 / clearRows / fixSharedFormulas:** 直接从 excel-exporter.js 复制（相同逻辑）。

**fillCharacterSheet(ws, d) 函数:** 填写款式 sheet，参数 ws = 工作表，d = loadData 结果。

1. **Header 区域:**
   - setVal(ws, 3, 3, "ROYAL REGENT PRODUCTS INDUSTRIES LIMITED") — Vendor name
   - setVal(ws, 3, 14, version.prepared_by || '') — Prepared by
   - setVal(ws, 4, 3, "SPIN MASTER TOYS FAR EAST LTD") — Customer
   - setVal(ws, 5, 3, product.item_no || '') — Material NO
   - setVal(ws, 5, 14, new Date()) — Date
   - setVal(ws, 7, 3, `${product.item_no || ''} - ${product.item_desc || ''}`) — Description

2. **Purchased Parts — Fabric Cost (R23-R35):** (EXP-02)
   - 汇率: `const rmbToUsd = parseFloat(params.hkd_usd) || (1/8.5);` 注意：hkd_usd 存的是 HKD/USD 比率。SPIN 需要 RMB→USD，简化用 `const exchangeRate = 8.5;`（后续可从 params 计算）
   - 实际换算：`const exchangeRate = (parseFloat(params.rmb_hkd) && parseFloat(params.hkd_usd)) ? (1 / parseFloat(params.rmb_hkd)) / parseFloat(params.hkd_usd) : 8.5;`
   - 但更简洁的方式：SPIN 的 material_price_rmb 直接除以一个 RMB/USD 近似汇率。用 `const rmbUsdRate = 8.5;` 作为默认值。
   - clearRows(ws, 23, 35, [3, 4, 10, 11]) — 先清空
   - fabricItems.slice(0, 12).forEach((item, i) => { const r = 23 + i; ... })
     - setVal(ws, r, 3, item.fabric_name || '') — English desc (col C)
     - setVal(ws, r, 4, item.fabric_name || '') — Chinese desc (col D)
     - const unitPriceUsd = r2((parseFloat(item.material_price_rmb) || 0) / rmbUsdRate)
     - setVal(ws, r, 10, unitPriceUsd) — Unit price USD (col J)
     - setVal(ws, r, 11, parseFloat(item.usage_amount) || 0) — Usage/qty (col K)
     - 不写 col L (col 12) — 它是 formula =J*K

3. **Purchased Parts — Others Cost (R60-R70):** (EXP-02 补充)
   - 暂不填写（SPIN 模板此区域留空），clearRows(ws, 60, 70, [3, 4, 10, 11])

4. **Packaging (R84-R97):** (EXP-03)
   - 从 packagingItems 中按 name 匹配:
     - H-tag: `item.name && item.name.toLowerCase().includes('h-tag')` → R86
     - CDU: `item.name && item.name.toLowerCase().includes('cdu')` → R87
     - Master carton / 外箱: `item.name && (item.name.includes('外箱') || item.name.toLowerCase().includes('master') || item.name.toLowerCase().includes('carton'))` → R92
   - 每行写入:
     - setVal(ws, row, 4, item.eng_name || item.name || '') — desc (col D)
     - setVal(ws, row, 10, r2(parseFloat(item.unit_price || item.new_price) / rmbUsdRate) || 0) — unit price USD
     - setVal(ws, row, 11, parseFloat(item.quantity) || 1) — qty
     - 不写 col L (formula)

5. **Labor (R126-R131):** (EXP-04)
   - 从 laborItems 中按 fabric_name 匹配:
     - contains '缝' or 'sew' (case insensitive) → R126 (Sewing)
     - contains '剪' or 'cut' → R129 (Cutting)
     - contains '包' or 'pack' → R128 (Packing)
     - contains '塞' or 'stuff' → R130 (Stuffing)
   - 每行写入:
     - const rateUsd = r2((parseFloat(item.material_price_rmb) || 0) / rmbUsdRate) — hourly rate
     - setVal(ws, row, 10, rateUsd) — rate (col J / col 10)
     - setVal(ws, row, 11, parseFloat(item.usage_amount) || 0) — hours/pc (col K / col 11)
     - 不写 col 12 (formula)

6. **Markup (R135-R139):** (EXP-05)
   - R135 (Material markup): setVal(ws, 135, 11, parseFloat(params.markup_material || params.markup_body) || 0.15) — markup %。注意：只写 C11 (col 11)，不写 C12 (formula)。C10 (col 10) 引用 subtotal，如果是 formula 则 setVal 会自动跳过。
   - R137 (Packaging markup): setVal(ws, 137, 11, parseFloat(params.markup_packaging) || 0.10)
   - R138 (Labor markup): setVal(ws, 138, 11, 0.15) — hardcoded
   - 不写 R135/R137/R138 col 12 (formula cells)

**fillSummary(ws, d) 函数:** 填写 Summary sheet。

- setVal(ws, 4, 3, "ROYAL REGENT PRODUCTS INDUSTRIES LIMITED") — Vendor name
- setVal(ws, 4, 14, version.prepared_by || '') — Prepared by
- setVal(ws, 5, 3, "SPIN MASTER TOYS FAR EAST LTD") — Customer
- setVal(ws, 6, 3, product.item_no || '') — Material NO
- setVal(ws, 8, 3, `${product.item_no || ''} - ${product.item_desc || ''}`) — Description
- R12 第一行: setVal(ws, 12, 1, product.item_no || ''), setVal(ws, 12, 2, product.item_desc || '')

**exportSpinVersion(versionId) 主函数:**

```javascript
async function exportSpinVersion(versionId) {
  const d = loadData(versionId);
  const wb = new ExcelJS.Workbook();
  await wb.xlsx.readFile(TEMPLATE_PATH);
  wb.calcProperties = { fullCalcOnLoad: true };
  fixSharedFormulas(wb);

  // Fill Summary sheet
  const summaryWs = wb.getWorksheet('Summary');
  if (summaryWs) fillSummary(summaryWs, d);

  // Fill first character sheet (non-Summary)
  const charWs = wb.worksheets.find(ws => ws.name !== 'Summary');
  if (charWs) fillCharacterSheet(charWs, d);

  return wb.xlsx.writeBuffer();
}

module.exports = { exportSpinVersion };
```
  </action>
  <verify>
    <automated>node -e "const m = require('./server/services/spin-exporter.js'); console.log(typeof m.exportSpinVersion === 'function' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - server/templates/VQ-template-spin.xlsx 文件存在
    - server/services/spin-exporter.js 导出 exportSpinVersion 函数
    - fillCharacterSheet 写入 Fabric (R23-R35 cols 3,4,10,11), Packaging (R86,R87,R92), Labor (R126-R130), Markup (R135,R137,R138 col 11)
    - fillSummary 写入 Summary sheet header 信息
    - 所有 formula cells 不被覆盖 (setVal 保护)
  </acceptance_criteria>
  <done>spin-exporter.js 可被 require 并调用 exportSpinVersion，模板文件就位</done>
</task>

</tasks>

<verification>
1. `node -e "require('./server/services/spin-exporter.js')"` 无报错
2. `ls server/templates/VQ-template-spin.xlsx` 文件存在
3. grep 确认 exportSpinVersion 函数导出
</verification>

<success_criteria>
- spin-exporter.js 完整实现所有 5 个区域的填充逻辑
- 模板文件已复制到 server/templates/
- 模块可被正常 require
</success_criteria>

<output>
After completion, create `.planning/phases/06-spin-export/06-01-SUMMARY.md`
</output>
