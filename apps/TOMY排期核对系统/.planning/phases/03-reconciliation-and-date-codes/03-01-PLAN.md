---
phase: 03-reconciliation-and-date-codes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - server/lib/dateCodeGenerator.ts
  - server/lib/dateCodeGenerator.test.ts
autonomous: true
requirements:
  - DATE-01
  - DATE-02
  - DATE-03
  - DATE-04
  - DATE-05

must_haves:
  truths:
    - "Date code D1526RR02 is generated for May 15 2026 PO走货期 with RR02 factory"
    - "Month letters A through L map correctly to Jan through Dec"
    - "subMonths correctly clamps end-of-month dates (Mar 31 → Feb 28)"
    - "Workday rollback skips weekends and Chinese public holidays"
    - "Spring Festival makeup Saturday (Feb 14, 2026) is treated as a workday"
    - "National Day (Oct 1, 2026) triggers rollback to Sep 30"
    - "Unknown factory codes return null, not a malformed date code"
  artifacts:
    - path: "server/lib/dateCodeGenerator.ts"
      provides: "Date code generation with workday rollback"
      exports: ["generateDateCode"]
    - path: "server/lib/dateCodeGenerator.test.ts"
      provides: "Tests covering all DATE requirements"
      min_lines: 40
  key_links:
    - from: "server/lib/dateCodeGenerator.ts"
      to: "chinese-days"
      via: "default import destructured to isWorkday, findWorkday"
      pattern: "import pkg from 'chinese-days'"
    - from: "server/lib/dateCodeGenerator.ts"
      to: "date-fns"
      via: "subMonths, parse, format"
      pattern: "import.*subMonths.*from 'date-fns'"
---

<objective>
Build the date code generator module with full test coverage using TDD.

Purpose: Date codes are a core deliverable — each PO item needs a production date code computed from the PO走货期 minus one calendar month, rolled back to a working day, formatted as monthLetter+day+2-digit-year+factoryCode.
Output: `server/lib/dateCodeGenerator.ts` with exported `generateDateCode()` function, fully tested.
</objective>

<execution_context>
@C:/Users/dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reconciliation-and-date-codes/03-RESEARCH.md

<interfaces>
<!-- Existing types from server/types/index.ts -->
From server/types/index.ts:
```typescript
export interface POItem {
  货号: string
  PO走货期: string      // "18 Mar 2026" format
  数量: number
  factoryCode: string   // "RR01" or "RR02"
  外箱: number | null
}
```

<!-- chinese-days CJS import pattern (CRITICAL — named imports fail in ESM) -->
```typescript
import pkg from 'chinese-days'
const { isWorkday, findWorkday } = pkg
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Date code generator with TDD</name>
  <files>server/lib/dateCodeGenerator.ts, server/lib/dateCodeGenerator.test.ts</files>
  <behavior>
    - generateDateCode('15 May 2026', 'RR02') returns "D1526RR02" (DATE-01, DATE-02)
    - generateDateCode('15 Jan 2026', 'RR01') returns month letter "A" for January (DATE-02)
    - generateDateCode('15 Dec 2026', 'RR02') returns month letter "L" for December after subtraction to November (DATE-02)
    - generateDateCode('31 Mar 2026', 'RR01') uses Feb 28 after subMonths clamping (DATE-03)
    - generateDateCode('15 Mar 2026', 'RR01') returns "B1426RR01" — Feb 15 is Sunday, rolls back to Feb 14 (makeup Saturday workday due to Spring Festival) (DATE-04)
    - generateDateCode('01 Nov 2026', 'RR02') returns "I3026RR02" — Oct 1 is National Day, rolls back to Sep 30 (DATE-04)
    - generateDateCode('15 May 2026', 'XX99') returns null for unknown factory code
    - generateDateCode('invalid date', 'RR01') returns null for unparseable date
    - generateDateCode('', 'RR01') returns null for empty string
  </behavior>
  <action>
RED: Create server/lib/dateCodeGenerator.test.ts with all test cases listed in behavior. Tests import generateDateCode from dateCodeGenerator. Run tests — they must fail (module does not exist yet).

GREEN: Create server/lib/dateCodeGenerator.ts implementing the algorithm:
1. Validate factoryCode matches /^RR0[12]$/; return null otherwise
2. Parse poZouHuoQiStr using date-fns `parse(str, 'd MMM yyyy', new Date())`; return null if NaN
3. Subtract one calendar month: `subMonths(poDate, 1)` from date-fns
4. Format to 'yyyy-MM-dd' string for chinese-days
5. Check `isWorkday(dateStr)` — if true, use that date; if false, call `findWorkday(-1, dateStr)` to get the nearest prior workday
6. Build result: `MONTH_LETTERS[workday.getMonth()] + workday.getDate() + String(workday.getFullYear()).slice(2) + factoryCode`

CRITICAL import pattern for chinese-days (CJS module in ESM):
```typescript
import pkg from 'chinese-days'
const { isWorkday, findWorkday } = pkg
```
Do NOT use named imports — they throw SyntaxError in ESM context.

const MONTH_LETTERS = 'ABCDEFGHIJKL'

REFACTOR: Ensure function is exported as named export. No refactoring needed if code is clean.
  </action>
  <verify>
    <automated>npx vitest run server/lib/dateCodeGenerator.test.ts</automated>
  </verify>
  <done>All 9+ test cases pass. generateDateCode correctly handles normal dates, end-of-month clamping, Spring Festival makeup workday, National Day rollback, unknown factory codes, and invalid dates.</done>
</task>

</tasks>

<verification>
npx vitest run server/lib/dateCodeGenerator.test.ts
</verification>

<success_criteria>
- generateDateCode exported and callable
- All DATE-01 through DATE-05 behaviors verified by tests
- chinese-days imported correctly (CJS default import pattern)
- Edge cases (holidays, weekends, invalid input) all covered
</success_criteria>

<output>
After completion, create `.planning/phases/03-reconciliation-and-date-codes/03-01-SUMMARY.md`
</output>
