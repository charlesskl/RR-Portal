---
phase: 04-output-download-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/zipBuilder.ts
  - server/lib/zipBuilder.test.ts
  - server/lib/summaryReport.ts
  - server/lib/summaryReport.test.ts
autonomous: true
requirements: [OUT-01, OUT-02, OUT-04]

must_haves:
  truths:
    - "buildZipBuffer() produces a valid ZIP archive containing all provided entries with correct folder paths"
    - "buildSummaryReport() lists every mismatched field with PO number, factory, field name, and both values"
    - "buildSummaryReport() lists every unmatched PO item with its factory and source file"
  artifacts:
    - path: "server/lib/zipBuilder.ts"
      provides: "buildZipBuffer() — archiver-based ZIP creation from in-memory buffers"
      exports: ["buildZipBuffer"]
    - path: "server/lib/zipBuilder.test.ts"
      provides: "ZIP builder unit tests covering folder structure and buffer validity"
    - path: "server/lib/summaryReport.ts"
      provides: "buildSummaryReport() — plain text summary from dual ReconciliationResult"
      exports: ["buildSummaryReport"]
    - path: "server/lib/summaryReport.test.ts"
      provides: "Summary report unit tests covering mismatch listing and unmatched items"
  key_links:
    - from: "server/lib/zipBuilder.ts"
      to: "archiver"
      via: "import archiver from 'archiver'"
      pattern: "archiver\\('zip'"
    - from: "server/lib/summaryReport.ts"
      to: "server/types/index.ts"
      via: "import ReconciliationResult"
      pattern: "ReconciliationResult"
---

<objective>
Create the ZIP builder and summary report library modules that Phase 4 needs for factory-split output packaging.

Purpose: These are the two new library functions that the upload route (Plan 02) will call to bundle annotated Excel files into a ZIP archive with a text summary report.

Output: Two tested library modules — `buildZipBuffer()` and `buildSummaryReport()` — ready for integration.
</objective>

<execution_context>
@C:/Users/dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-output-download-and-integration/04-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from the existing codebase -->

From server/types/index.ts:
```typescript
export interface FieldMismatch {
  field: string
  scheduleValue: unknown
  poValue: unknown
}

export interface RowMatchResult {
  scheduleRowIndex: number
  tomyPO: string
  货号: string
  status: 'matched' | 'unmatched' | 'ambiguous'
  mismatches: FieldMismatch[]
  dateCode: string | null
  sourceFile: string
}

export interface ReconciliationResult {
  matched: RowMatchResult[]
  unmatchedPOItems: Array<{ tomyPO: string; 货号: string; sourceFile: string; poItem: POItem; poData: POData }>
  ambiguousPOItems: Array<{ tomyPO: string; 货号: string; sourceFile: string; count: number }>
  errors: string[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ZIP builder module with TDD</name>
  <files>server/lib/zipBuilder.ts, server/lib/zipBuilder.test.ts</files>
  <behavior>
    - Test 1: buildZipBuffer with two entries returns a Buffer that starts with PK zip signature (0x50, 0x4b)
    - Test 2: buildZipBuffer entries with folder paths (e.g., "DG/file.xlsx", "ID/file.xlsx") are preserved — extract and verify entry names
    - Test 3: buildZipBuffer with an empty entries array returns a valid (empty) ZIP buffer
    - Test 4: buildZipBuffer entry buffers are byte-identical when extracted (round-trip fidelity)
  </behavior>
  <action>
First install archiver as a real dependency and its types as devDependency:
```bash
npm install archiver && npm install --save-dev @types/archiver
```

RED phase: Create `server/lib/zipBuilder.test.ts` with the 4 tests above. Use `jszip` (already in node_modules) in the test to decompress and verify the ZIP output from `buildZipBuffer()`. Run tests — all must fail.

GREEN phase: Create `server/lib/zipBuilder.ts` implementing `buildZipBuffer()`:
- Accepts `Array<{ name: string; buffer: Buffer }>` and returns `Promise<Buffer>`
- Uses `archiver('zip', { zlib: { level: 6 } })` piped to a `PassThrough` stream
- Collects chunks via `pass.on('data', ...)` and resolves on `pass.on('end', ...)`
- For each entry, calls `archive.append(entry.buffer, { name: entry.name })`
- Calls `archive.finalize()` after all entries appended
- Rejects on `archive.on('error', ...)` or `pass.on('error', ...)`
- Also handles `archive.on('warning', ...)` — reject unless code is ENOENT

Run tests — all must pass.

IMPORTANT: Use `import archiver from 'archiver'` (default import). The `.js` extension is needed for the project's ESM-style imports: `import archiver from 'archiver'` (archiver is a CJS package, so no `.js` needed on the import specifier itself — but verify in existing code style; other imports like `'./normalize.js'` use `.js` for local files only).
  </action>
  <verify>
    <automated>npx vitest run server/lib/zipBuilder.test.ts</automated>
  </verify>
  <done>buildZipBuffer() produces valid ZIP archives with correct folder structure; 4 tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Summary report module with TDD</name>
  <files>server/lib/summaryReport.ts, server/lib/summaryReport.test.ts</files>
  <behavior>
    - Test 1: Report header contains "TOMY 排期核对汇总报告" and generation timestamp
    - Test 2: Matched rows with mismatches appear with PO number, factory label, field name, scheduleValue and poValue
    - Test 3: Unmatched PO items appear with PO number, 货号, sourceFile, and factory label
    - Test 4: When both results have zero mismatches and zero unmatched, report shows "共 0 条" for both sections
    - Test 5: Factory labels use "RR01/东莞" for dgResult and "RR02/印尼" for idResult
  </behavior>
  <action>
RED phase: Create `server/lib/summaryReport.test.ts` with the 5 tests above. Build mock `ReconciliationResult` objects with known mismatches and unmatched items. Run tests — all must fail.

GREEN phase: Create `server/lib/summaryReport.ts` implementing `buildSummaryReport()`:
- Signature: `buildSummaryReport(dgResult: ReconciliationResult, idResult: ReconciliationResult): string`
- Import `ReconciliationResult` from `'../types/index.js'`
- Build header: title line, generation timestamp (`new Date().toLocaleString('zh-CN')`), separator
- Section 1 — Field mismatches: filter `matched` where `mismatches.length > 0` from both results, tag with factory label ("RR01/东莞" or "RR02/印尼"), list each with PO number, 货号, factory, and each mismatch field/values
- Section 2 — Unmatched PO items: combine `unmatchedPOItems` from both results, tag with factory label, list each with PO, 货号, sourceFile, factory
- Section 3 — Summary counts at end: total mismatched rows, total unmatched items, total errors
- Return `lines.join('\n')`

Handle edge case: if either dgResult or idResult is null/undefined (one factory not uploaded), treat as empty result. Use a helper: `const emptyResult: ReconciliationResult = { matched: [], unmatchedPOItems: [], ambiguousPOItems: [], errors: [] }`

Run tests — all must pass.
  </action>
  <verify>
    <automated>npx vitest run server/lib/summaryReport.test.ts</automated>
  </verify>
  <done>buildSummaryReport() produces correct plain-text summary with all discrepancy details; 5 tests green</done>
</task>

</tasks>

<verification>
After both tasks complete:
```bash
npx vitest run server/lib/zipBuilder.test.ts server/lib/summaryReport.test.ts
```
All 9 tests green. Full suite still passes:
```bash
npx vitest run
```
</verification>

<success_criteria>
- buildZipBuffer() creates valid ZIP archives from in-memory buffers with folder paths preserved
- buildSummaryReport() generates a text report listing all mismatches and unmatched items per factory
- archiver is in package.json dependencies; @types/archiver in devDependencies
- All new tests pass; all existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-download-and-integration/04-01-SUMMARY.md`
</output>
