# 生产管控中心 - 环境变量模板
# 复制为 .env 并填入实际值后再启动

# HTTP 端口
PORT=3300

# CORS 允许的源（生产环境一般留空走同源）
# CORS_ORIGIN=http://localhost:5173

# 数据目录（SQLite db + WAL 日志会写到这里）
# DATA_PATH=/app/data

# JWT 配置（生产环境务必改成长随机字符串）
JWT_SECRET=change-me-to-a-long-random-string-in-production
JWT_EXPIRES=8h
