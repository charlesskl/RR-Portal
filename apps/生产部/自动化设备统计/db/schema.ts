import { integer, real, sqliteTable, text } from 'drizzle-orm/sqlite-core';

export const equipment = sqliteTable('equipment', { id: integer('id').primaryKey({ autoIncrement: true }), factory: text('factory').notNull(), workshop: text('workshop').notNull(), name: text('name').notNull(), quantity: integer('quantity').notNull(), unitPrice: real('unit_price').notNull(), investment: real('investment').notNull(), maOrder: real('ma_order').notNull().default(0), productionOrders: real('production_orders').notNull().default(0), savedCost: real('saved_cost').notNull().default(0), balance: real('balance').notNull().default(0), unitSaving: real('unit_saving').notNull().default(0), updatedAt: text('updated_at').notNull() });

export const productionRecords = sqliteTable('production_records', { id: integer('id').primaryKey({ autoIncrement: true }), productionDate: text('production_date').notNull(), factory: text('factory').notNull(), workshop: text('workshop').notNull(), equipmentName: text('equipment_name').notNull(), productionLine: text('production_line').notNull().default(''), productionQuantity: integer('production_quantity').notNull(), operator: text('operator').notNull(), note: text('note').notNull().default(''), createdAt: text('created_at').notNull() });

export const appUsers = sqliteTable('app_users', { id: integer('id').primaryKey({ autoIncrement: true }), name: text('name').notNull(), account: text('account').notNull().unique(), role: text('role').notNull(), department: text('department').notNull(), workshop: text('workshop').notNull(), enabled: integer('enabled', { mode: 'boolean' }).notNull().default(true) });
