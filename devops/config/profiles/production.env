# Production environment — Alibaba Cloud ECS
DEPLOY_SERVER=root@8.148.146.194
DEPLOY_COMPOSE_PATH=/opt/rr-portal/docker-compose.cloud.yml

# Nginx basic auth (for automated QA testing)
# SECURITY: Do not commit real credentials. Set via CI/CD secrets or local override.
NGINX_AUTH_USER=${NGINX_AUTH_USER:?Set NGINX_AUTH_USER in CI/CD secrets}
NGINX_AUTH_PASS=${NGINX_AUTH_PASS:?Set NGINX_AUTH_PASS in CI/CD secrets - use a strong password}
